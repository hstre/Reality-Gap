---
interface Props {
  lang: string;
  base: string;
  altLangPath?: string;
  altLang?: string;
}

const { lang, base, altLangPath, altLang } = Astro.props;

const isDE = lang === 'de';

const navLinks = isDE
  ? [
      { href: `${base}/de/`, label: 'Startseite' },
      { href: `${base}/de/methodik`, label: 'Methodik' },
      { href: `${base}/de/paper`, label: 'Paper' },
      { href: `${base}/de/unternehmen`, label: 'Unternehmen' },
      { href: `${base}/de/rankings`, label: 'Rankings' },
      { href: `${base}/de/forschung`, label: 'Forschung' },
      { href: `${base}/de/makro`, label: 'Makro' },
      { href: `${base}/de/ueber`, label: 'Über' },
    ]
  : [
      { href: `${base}/`, label: 'Home' },
      { href: `${base}/methodology`, label: 'Methodology' },
      { href: `${base}/paper`, label: 'Paper' },
      { href: `${base}/companies`, label: 'Companies' },
      { href: `${base}/rankings`, label: 'Rankings' },
      { href: `${base}/research`, label: 'Research' },
      { href: `${base}/china`, label: 'China' },
      { href: `${base}/macro`, label: 'Macro' },
      { href: `${base}/about`, label: 'About' },
    ];

const currentPath = Astro.url.pathname;

const altLangHref = altLangPath
  ? `${base}${altLangPath}`
  : isDE
    ? `${base}/`
    : `${base}/de/`;

const altLangLabel = isDE ? 'EN' : 'DE';
---

<header class="border-b border-gray-200 bg-white">
  <div class="max-w-5xl mx-auto px-6 flex items-center justify-between h-14">
    <!-- Logo mark -->
    <a
      href={isDE ? `${base}/de/` : `${base}/`}
      class="text-gray-900 hover:opacity-80 transition-opacity no-underline flex items-center"
      aria-label="Reality Gap — Home"
    >
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 220 115"
           class="h-8 w-auto" fill="none" aria-hidden="true">
        <!-- R stem -->
        <rect x="0" y="0" width="15" height="112" fill="currentColor"/>
        <!-- R bowl (even-odd → transparent interior) -->
        <path fill-rule="evenodd" fill="currentColor" d="
          M 15,0 H 54 Q 91,0 91,30 Q 91,60 54,60 H 15 Z
          M 21,10 H 51 Q 75,10 75,30 Q 75,50 51,50 H 21 Z
        "/>
        <!-- R leg -->
        <line x1="50" y1="62" x2="95" y2="112"
              stroke="currentColor" stroke-width="16" stroke-linecap="round"/>
        <!-- G ring (CW outer + CCW inner → nonzero fill = transparent hollow) -->
        <path fill="currentColor" d="
          M 191,14 A 50,50 0 1 1 191,98 L 183,86 A 35,35 0 1 0 183,26 Z
        "/>
        <!-- G arm -->
        <rect x="162" y="47" width="53" height="18" fill="currentColor"/>
      </svg>
    </a>

    <!-- Desktop nav -->
    <nav class="hidden md:flex items-center gap-6" aria-label="Main navigation">
      {navLinks.map(link => {
        const isActive = currentPath === link.href || currentPath === link.href + '/';
        return (
          <a
            href={link.href}
            class:list={[
              'text-sm no-underline transition-colors',
              isActive
                ? 'text-accent font-medium'
                : 'text-gray-600 hover:text-gray-900',
            ]}
          >
            {link.label}
          </a>
        );
      })}
      <a
        href={altLangHref}
        class="text-xs font-medium text-gray-400 hover:text-gray-700 border border-gray-200 rounded px-2 py-0.5 no-underline ml-2"
        title={altLangLabel === 'DE' ? 'Deutsche Version' : 'English version'}
      >
        {altLangLabel}
      </a>
    </nav>

    <!-- Mobile: language + hamburger -->
    <div class="flex md:hidden items-center gap-3">
      <a
        href={altLangHref}
        class="text-xs font-medium text-gray-400 border border-gray-200 rounded px-2 py-0.5 no-underline"
      >
        {altLangLabel}
      </a>
      <button
        id="menu-toggle"
        aria-label="Toggle navigation"
        class="text-gray-600 hover:text-gray-900 p-1"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16"/>
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile nav drawer -->
  <div id="mobile-nav" class="hidden md:hidden border-t border-gray-100 bg-white">
    <nav class="max-w-5xl mx-auto px-6 py-4 flex flex-col gap-3">
      {navLinks.map(link => (
        <a
          href={link.href}
          class="text-sm text-gray-700 hover:text-accent no-underline py-1"
        >
          {link.label}
        </a>
      ))}
    </nav>
  </div>
</header>

<script>
  const toggle = document.getElementById('menu-toggle');
  const mobileNav = document.getElementById('mobile-nav');
  if (toggle && mobileNav) {
    toggle.addEventListener('click', () => {
      mobileNav.classList.toggle('hidden');
    });
  }
</script>
